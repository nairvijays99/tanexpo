{
  "extends": "../../tsconfig.json",
  "include": ["src"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    "types": ["react", "react-native"]  // Explicitly include only needed types
  }
}